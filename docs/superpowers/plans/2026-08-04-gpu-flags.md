# GPU-Flags mit Auto-Rollback — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** TwitchDual setzt beim Start ein vollstaendiges Chromium-GPU-Flag-Paket und faellt automatisch auf Electron-Defaults zurueck, wenn ein Start damit nicht ueberlebt wird.

**Architecture:** Die gesamte Entscheidungslogik liegt in `src/gpu-flags.js` als reine Funktionen ohne Electron-Import — dadurch mit `node --test` pruefbar. `main.js` bekommt nur duenne Verdrahtung: Flags auf Modulebene anwenden (vor `app.whenReady()`), Zustand in `electron-store` fortschreiben, Absturz- und Erfolgs-Ereignisse melden.

**Tech Stack:** Electron 33, electron-store 8, `node --test` (Node-eigener Testrunner), CommonJS.

**Spec:** `docs/superpowers/specs/2026-08-04-gpu-flags-design.md`
**Branch:** `feat/gpu-flags`

## Global Constraints

- Modul-Stil: CommonJS (`require` / `module.exports`), passend zu allen 15 Dateien in `src/`.
- Kommentare auf Deutsch, **ohne Umlaute** (`oe`, `ue`, `ae`) — so wie im gesamten Bestandscode.
- `src/gpu-flags.js` darf **niemals** `require('electron')` aufrufen. Die Testbarkeit haengt daran.
- Die exakten fuenf Switches, in dieser Reihenfolge: `ignore-gpu-blocklist`, `enable-gpu-rasterization`, `enable-zero-copy`, `enable-hardware-overlays`, `disable-gpu-driver-bug-workarounds`.
- `mode: 'safe'` bedeutet **keine Flags setzen**. Es bedeutet ausdruecklich *nicht* `app.disableHardwareAcceleration()`.
- Die Umgebungsvariable heisst exakt `TWITCHDUAL_NO_GPU` und wirkt nur beim Wert `'1'`.
- Der electron-store-Schluessel heisst exakt `gpuState`.
- Tests laufen mit `npm test` (= `node --test`). Die bestehende Suite muss vor und nach jeder Task gruen sein.

---

### Task 1: Zustandslogik in `src/gpu-flags.js`

**Files:**
- Create: `src/gpu-flags.js`
- Test: `test/gpu-flags.test.js`

**Interfaces:**
- Consumes: nichts (erste Task).
- Produces:
  - `SWITCHES: string[]` — die fuenf Switch-Namen ohne fuehrende Bindestriche.
  - `DEFAULT_STATE: { mode: 'accel', pending: false }`
  - `decideMode(state, env) -> 'accel' | 'safe'`
  - `nextState(state, event) -> { mode, pending }`, wobei `event` eines von `'start-accel' | 'start-safe' | 'render-ok' | 'gpu-crash'` ist.

- [ ] **Step 1: Den fehlschlagenden Test schreiben**

Datei `test/gpu-flags.test.js`:

```js
const { test } = require('node:test');
const assert = require('node:assert');
const { SWITCHES, DEFAULT_STATE, decideMode, nextState } = require('../src/gpu-flags');

test('SWITCHES enthaelt genau die fuenf dokumentierten Switches', () => {
  assert.deepEqual(SWITCHES, [
    'ignore-gpu-blocklist',
    'enable-gpu-rasterization',
    'enable-zero-copy',
    'enable-hardware-overlays',
    'disable-gpu-driver-bug-workarounds'
  ]);
});

test('Erststart ohne gespeicherten Zustand -> accel', () => {
  assert.equal(decideMode(undefined, {}), 'accel');
  assert.equal(decideMode(null, {}), 'accel');
});

test('sauberer Vorstart -> accel', () => {
  assert.equal(decideMode({ mode: 'accel', pending: false }, {}), 'accel');
});

test('offene pending-Marke -> safe (voriger Start hat nicht ueberlebt)', () => {
  assert.equal(decideMode({ mode: 'accel', pending: true }, {}), 'safe');
});

test('einmal safe, immer safe', () => {
  assert.equal(decideMode({ mode: 'safe', pending: false }, {}), 'safe');
});

test('TWITCHDUAL_NO_GPU=1 ueberschreibt jeden gespeicherten Zustand', () => {
  const env = { TWITCHDUAL_NO_GPU: '1' };
  assert.equal(decideMode({ mode: 'accel', pending: false }, env), 'safe');
  assert.equal(decideMode(DEFAULT_STATE, env), 'safe');
});

test('TWITCHDUAL_NO_GPU mit anderem Wert wirkt nicht', () => {
  assert.equal(decideMode({ mode: 'accel', pending: false }, { TWITCHDUAL_NO_GPU: '0' }), 'accel');
});

test('nextState: start-accel setzt die pending-Marke', () => {
  assert.deepEqual(nextState(DEFAULT_STATE, 'start-accel'), { mode: 'accel', pending: true });
});

test('nextState: start-safe merkt safe dauerhaft', () => {
  assert.deepEqual(nextState({ mode: 'accel', pending: true }, 'start-safe'), { mode: 'safe', pending: false });
});

test('nextState: render-ok loescht die Marke, behaelt den Modus', () => {
  assert.deepEqual(nextState({ mode: 'accel', pending: true }, 'render-ok'), { mode: 'accel', pending: false });
});

test('nextState: gpu-crash zwingt auf safe', () => {
  assert.deepEqual(nextState({ mode: 'accel', pending: true }, 'gpu-crash'), { mode: 'safe', pending: false });
});

test('nextState ist idempotent', () => {
  for (const ev of ['start-accel', 'start-safe', 'render-ok', 'gpu-crash']) {
    const once = nextState(DEFAULT_STATE, ev);
    assert.deepEqual(nextState(once, ev), once, `nicht idempotent: ${ev}`);
  }
});

test('nextState ignoriert unbekannte Ereignisse', () => {
  assert.deepEqual(nextState({ mode: 'accel', pending: true }, 'quatsch'), { mode: 'accel', pending: true });
});
```

- [ ] **Step 2: Test laufen lassen und Fehlschlag bestaetigen**

Run: `npm test`
Expected: FAIL — `Cannot find module '../src/gpu-flags'`

- [ ] **Step 3: Minimale Implementierung schreiben**

Datei `src/gpu-flags.js`:

```js
// GPU-Flag-Entscheidung fuer TwitchDual.
//
// Bewusst ohne require('electron') — die Logik muss mit `node --test`
// pruefbar bleiben. main.js reicht Electron-Objekte von aussen herein.

// Reihenfolge ist dokumentiert (siehe Spec) und wird vom Test festgenagelt.
const SWITCHES = [
  'ignore-gpu-blocklist',
  'enable-gpu-rasterization',
  'enable-zero-copy',
  'enable-hardware-overlays',
  'disable-gpu-driver-bug-workarounds'
];

const DEFAULT_STATE = { mode: 'accel', pending: false };

// 'safe' heisst: keine Flags setzen. Nicht: Hardwarebeschleunigung abschalten.
function decideMode(state, env) {
  if (env && env.TWITCHDUAL_NO_GPU === '1') return 'safe';
  const s = state || DEFAULT_STATE;
  if (s.mode === 'safe') return 'safe';
  // Offene Marke = der vorige Start hat das Rendern nicht ueberlebt.
  if (s.pending) return 'safe';
  return 'accel';
}

function nextState(state, event) {
  const s = { ...DEFAULT_STATE, ...(state || {}) };
  switch (event) {
    case 'start-accel': return { mode: 'accel', pending: true };
    case 'start-safe': return { mode: 'safe', pending: false };
    case 'render-ok': return { mode: s.mode, pending: false };
    case 'gpu-crash': return { mode: 'safe', pending: false };
    default: return s;
  }
}

module.exports = { SWITCHES, DEFAULT_STATE, decideMode, nextState };
```

- [ ] **Step 4: Test laufen lassen und Erfolg bestaetigen**

Run: `npm test`
Expected: PASS — alle neuen Tests gruen, die bestehenden 19 Testdateien unveraendert gruen.

- [ ] **Step 5: Committen**

```bash
git add src/gpu-flags.js test/gpu-flags.test.js
git commit -m "feat: Zustandslogik fuer GPU-Flags mit Rollback-Marke"
```

---

### Task 2: `applyGpuFlags` — Flags setzen ohne Electron-Abhaengigkeit

**Files:**
- Modify: `src/gpu-flags.js`
- Test: `test/gpu-flags.test.js`

**Interfaces:**
- Consumes: `SWITCHES`, `decideMode`, `nextState` aus Task 1.
- Produces: `applyGpuFlags({ commandLine, state, env, log }) -> { mode, pending }`
  - `commandLine`: Objekt mit `appendSwitch(name)` — in Produktion `app.commandLine`, im Test ein Doppelgaenger.
  - `log`: Funktion `(event, detail) => void`.
  - Rueckgabe ist der Zustand, den der Aufrufer persistieren muss.

- [ ] **Step 1: Den fehlschlagenden Test schreiben**

Den bestehenden `require`-Aufruf am Dateikopf von `test/gpu-flags.test.js` um
`applyGpuFlags` erweitern:

```js
const { SWITCHES, DEFAULT_STATE, decideMode, nextState, applyGpuFlags } = require('../src/gpu-flags');
```

Dann ans Dateiende anhaengen:

```js
function fakeCommandLine() {
  const applied = [];
  return { applied, appendSwitch: (name) => applied.push(name) };
}

test('applyGpuFlags setzt im accel-Modus alle fuenf Switches', () => {
  const cl = fakeCommandLine();
  const st = applyGpuFlags({ commandLine: cl, state: DEFAULT_STATE, env: {}, log: () => {} });
  assert.deepEqual(cl.applied, SWITCHES);
  assert.deepEqual(st, { mode: 'accel', pending: true });
});

test('applyGpuFlags setzt im safe-Modus keinen einzigen Switch', () => {
  const cl = fakeCommandLine();
  const st = applyGpuFlags({
    commandLine: cl,
    state: { mode: 'accel', pending: true },
    env: {},
    log: () => {}
  });
  assert.deepEqual(cl.applied, []);
  assert.deepEqual(st, { mode: 'safe', pending: false });
});

test('applyGpuFlags respektiert TWITCHDUAL_NO_GPU=1', () => {
  const cl = fakeCommandLine();
  applyGpuFlags({
    commandLine: cl,
    state: DEFAULT_STATE,
    env: { TWITCHDUAL_NO_GPU: '1' },
    log: () => {}
  });
  assert.deepEqual(cl.applied, []);
});

test('applyGpuFlags protokolliert den gewaehlten Modus', () => {
  const seen = [];
  applyGpuFlags({
    commandLine: fakeCommandLine(),
    state: DEFAULT_STATE,
    env: {},
    log: (event, detail) => seen.push([event, detail])
  });
  assert.deepEqual(seen, [['gpu-mode', 'accel']]);
});
```

- [ ] **Step 2: Test laufen lassen und Fehlschlag bestaetigen**

Run: `npm test`
Expected: FAIL — `applyGpuFlags is not a function`

- [ ] **Step 3: Minimale Implementierung schreiben**

In `src/gpu-flags.js` vor `module.exports` einfuegen:

```js
// Wendet die Flags an und liefert den Zustand zurueck, den der Aufrufer
// speichern muss. Kennt weder electron-store noch app — beides kommt von aussen.
function applyGpuFlags({ commandLine, state, env, log }) {
  const mode = decideMode(state, env);
  if (mode === 'accel') {
    for (const name of SWITCHES) commandLine.appendSwitch(name);
  }
  log('gpu-mode', mode);
  return nextState(state, mode === 'accel' ? 'start-accel' : 'start-safe');
}
```

Und den Export erweitern:

```js
module.exports = { SWITCHES, DEFAULT_STATE, decideMode, nextState, applyGpuFlags };
```

- [ ] **Step 4: Test laufen lassen und Erfolg bestaetigen**

Run: `npm test`
Expected: PASS

- [ ] **Step 5: Committen**

```bash
git add src/gpu-flags.js test/gpu-flags.test.js
git commit -m "feat: applyGpuFlags setzt Switches ueber injizierte commandLine"
```

---

### Task 3: Verdrahtung in `main.js`

**Files:**
- Modify: `main.js` (Kopf nach Zeile 19, Store-Defaults ab Zeile 21, `createWindows()` ab Zeile 85, `whenReady`-Block ab Zeile 468)

**Interfaces:**
- Consumes: `applyGpuFlags`, `nextState` aus `src/gpu-flags.js`.
- Produces: nichts fuer spaetere Tasks.

**Reihenfolge-Einschraenkung:** `app.commandLine.appendSwitch()` wirkt nur vor
`app.whenReady()`. Der Aufruf muss auf Modulebene stehen, direkt nach der
Store-Erzeugung — **nicht** in `createWindows()` und **nicht** im
`whenReady`-Block. `updaterLog` ist eine `function`-Deklaration (`main.js:451`)
und damit gehoisted, also von oben aufrufbar.

- [ ] **Step 1: Import und Store-Default ergaenzen**

Nach `main.js:19` (`const { safeStorage } = require('electron');`) einfuegen:

```js
const { applyGpuFlags, nextState } = require('./src/gpu-flags');
```

In den `Store`-Defaults (`main.js:22-32`) als weiteren Eintrag ergaenzen:

```js
    gpuState: { mode: 'accel', pending: false },
```

- [ ] **Step 2: Flags auf Modulebene anwenden**

Direkt nach dem `const store = new Store({ ... });`-Block (endet `main.js:33`) einfuegen:

```js
// --- GPU-Flags (v1.9.0) ----------------------------------------------------
// Muss vor app.whenReady() laufen, sonst ignoriert Chromium die Switches.
// Der Zustand mit offener pending-Marke bedeutet: der vorige Start hat das
// Rendern nicht ueberlebt -> dieser Start laeuft ohne Flags.
store.set('gpuState', applyGpuFlags({
  commandLine: app.commandLine,
  state: store.get('gpuState'),
  env: process.env,
  log: updaterLog
}));
```

- [ ] **Step 3: Erfolgs- und Absturzmeldungen verdrahten**

In `createWindows()` nach `videoWin.loadURL(...)` (`main.js:115`) einfuegen:

```js
  // Marke erst loeschen, wenn das Video-Fenster 20 s stabil gerendert hat.
  videoWin.webContents.once('did-frame-finish-load', () => {
    setTimeout(() => {
      if (!videoWin || videoWin.isDestroyed()) return;
      store.set('gpuState', nextState(store.get('gpuState'), 'render-ok'));
      updaterLog('gpu-render-ok');
    }, 20000).unref?.();
  });
```

Im `whenReady`-Block (`main.js:468`) nach `createWindows();` einfuegen:

```js
  updaterLog('gpu-status', JSON.stringify(app.getGPUFeatureStatus()));

  const forceSafe = (grund) => {
    store.set('gpuState', nextState(store.get('gpuState'), 'gpu-crash'));
    updaterLog('gpu-crash', grund);
  };
  app.on('child-process-gone', (_e, details) => {
    if (details && details.type === 'GPU') forceSafe(`gpu:${details.reason}`);
  });
  app.on('render-process-gone', (_e, _wc, details) => {
    forceSafe(`renderer:${details && details.reason}`);
  });
```

- [ ] **Step 4: Bestehende Suite pruefen**

Run: `npm test`
Expected: PASS — `main.js` hat keine eigenen Tests, aber nichts darf brechen.

- [ ] **Step 5: Rauchtest im accel-Modus**

Run: `npm start`
Expected:
- Beide Fenster oeffnen, ein Stream laedt, **der Chat ist weiterhin transparent** (Glaseffekt aus v1.7.0 intakt).
- In `%APPDATA%\twitchdual\updater.log` steht `update:gpu-mode accel` und eine `update:gpu-status`-Zeile.
- In der `gpu-status`-Zeile das Feld `video_decode` ablesen und notieren: `enabled_on` = GPU dekodiert, `software_only` = weiterhin CPU.
- Nach ~20 s erscheint `update:gpu-render-ok`.

Bricht hier etwas (schwarzes Fenster, Flackern im Chat): App schliessen, neu starten — der naechste Start muss laut Log `gpu-mode safe` melden. Genau das ist der Rollback.

- [ ] **Step 6: Rollback-Test erzwingen**

Die Marke von Hand auf "haengengeblieben" setzen und pruefen, dass der Rollback greift:

1. App schliessen.
2. `%APPDATA%\twitchdual\config.json` oeffnen, unter `gpuState` den Wert
   `"pending"` auf `true` setzen, speichern.
3. `npm start`

Expected: Das Log meldet `update:gpu-mode safe`, die App startet normal, und
`gpuState` steht danach auf `{ "mode": "safe", "pending": false }`.

Zuruecksetzen fuer den Normalbetrieb: App schliessen, in `config.json`
`gpuState` auf `{ "mode": "accel", "pending": false }` setzen.

- [ ] **Step 7: Not-Aus testen**

Run: `set TWITCHDUAL_NO_GPU=1 && npm start` (cmd) bzw. `$env:TWITCHDUAL_NO_GPU='1'; npm start` (PowerShell)
Expected: Log meldet `update:gpu-mode safe`, obwohl `gpuState.mode` auf `accel` steht.

- [ ] **Step 8: Committen**

```bash
git add main.js
git commit -m "feat: GPU-Flags in main.js verdrahtet, mit Auto-Rollback"
```

---

### Task 4: Version, Doku, Release-Vorbereitung

**Files:**
- Modify: `package.json:3`
- Modify: `docs/TODO.md`

**Interfaces:**
- Consumes: nichts.
- Produces: nichts.

- [ ] **Step 1: Version anheben**

In `package.json` Zeile 3: `"version": "1.8.4"` → `"version": "1.9.0"`

- [ ] **Step 2: `docs/TODO.md` ergaenzen**

Vor dem Abschnitt `## Releases / Auto-Update (seit v1.0.0)` einfuegen:

```markdown
## GPU-Beschleunigung (v1.9.0)

- `src/gpu-flags.js` setzt beim Start fuenf Chromium-Switches:
  `ignore-gpu-blocklist`, `enable-gpu-rasterization`, `enable-zero-copy`,
  `enable-hardware-overlays`, `disable-gpu-driver-bug-workarounds`.
- **Auto-Rollback:** Vor dem Setzen wird eine `pending`-Marke in
  `gpuState` (electron-store) geschrieben, die erst 20 s nach dem ersten
  gerenderten Frame geloescht wird. Findet der naechste Start die Marke
  noch vor, laeuft er ohne Flags und merkt `safe` dauerhaft.
  GPU- und Renderer-Abstuerze setzen sofort auf `safe`.
- **Not-Aus:** `TWITCHDUAL_NO_GPU=1` erzwingt `safe`, egal was gespeichert ist.
- `safe` heisst *keine Flags* — Electrons Hardwarebeschleunigung bleibt an.
- **Nachweis:** `updater.log` enthaelt beim Start `gpu-mode <modus>` und
  `gpu-status` mit `app.getGPUFeatureStatus()`. Das Feld `video_decode`
  zeigt, ob die GPU den Twitch-Stream wirklich dekodiert.
- **Achtung Chat-Transparenz:** Das Chat-Fenster laeuft mit
  `transparent: true` (main.js:106). `enable-hardware-overlays` und
  `disable-gpu-driver-bug-workarounds` sind die bekannten Stoerenfriede.
  Bringen sie laut Messung nichts, gehoeren sie entfernt.
```

- [ ] **Step 3: Suite ein letztes Mal pruefen**

Run: `npm test`
Expected: PASS

- [ ] **Step 4: Committen**

```bash
git add package.json docs/TODO.md
git commit -m "chore: v1.9.0 — GPU-Flags dokumentiert"
```

---

## Messphase (nach Task 4, vor dem Release)

Diese Schritte sind Messung, kein Code. Ergebnis entscheidet, ob die zwei
riskanten Flags drin bleiben.

- [ ] **Basiswert:** `TWITCHDUAL_NO_GPU=1`, zwei Streams laden, CPU-Last aller
  TwitchDual-Prozesse ueber 5 Minuten im Task-Manager (Details-Tab, nach Name
  gruppiert) ablesen und notieren.
- [ ] **Vollausbau:** normal starten, gleiche Messung, plus `video_decode` aus
  dem Log.
- [ ] **Vergleich:** Keine messbare CPU-Senkung → die Flags sind wirkungslos,
  `enable-hardware-overlays` und `disable-gpu-driver-bug-workarounds` wieder aus
  `SWITCHES` entfernen (Test in Task 1 mit anpassen).
- [ ] **Zurueckschneiden:** Bei Gewinn die beiden riskanten Flags einzeln
  entfernen und nachmessen. Was nichts bringt, bleibt draussen.
- [ ] **Eigentliches Ziel:** League-Match ab Minute 25 spielen, FPS beobachten.
  Das ist das Kriterium, das zaehlt — die CPU-Messung ist nur der Zwischenschritt.

## Release (erst nach der Messphase)

Nach dem in `docs/TODO.md` dokumentierten 4-Schritte-Weg: `npm run dist`,
EXE + Blockmap auf Bindestrich-Namen kopieren, `gh release create v1.9.0 ...`.
