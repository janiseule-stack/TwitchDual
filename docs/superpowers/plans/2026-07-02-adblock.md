# TwitchDual Werbe-Blocker (v1.2.0) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Twitch-Werbung im Video-Fenster überspringen (vaft-Injektion in den Player-iframe) und, wenn doch Werbung durchkommt, per Overlay + Mute überbrücken — abschaltbar über einen persistenten Adblock-Schalter.

**Architecture:** vaft (gepinntes Fremdskript in `vendor/vaft.js`) wird vom Preload in den `player.twitch.tv`-iframe injiziert und wechselt bei Werbung auf eine werbefreie Playlist. Ein kleiner Wrapper meldet Werbe-Start/-Ende per `window.postMessage` → Preload (Isolated World) → IPC `adblock-state` → Main → Video-Fenster. Dort steuert eine DOM-freie Zustandsmaschine (`renderer/lib/ad-overlay-state.js`, unit-getestet) Overlay-Sichtbarkeit und Mute, mit 120-s-Watchdog. Ein Schalter (Default an, in electron-store) schaltet die gesamte Injektion ab → Verhalten exakt wie v1.1.0.

**Tech Stack:** Electron 33, electron-store 8, `node --test` (kein externes Test-Framework), UMD-Module in `renderer/lib/` (laufen im Browser via `<script>` und unter Node).

## Global Constraints

- `npm test` (`node --test`) muss **vor und nach jeder Task** grün bleiben. Baseline: **59 Tests**. Neue Tests kommen additiv dazu.
- Tests bleiben **datenstand-unabhängig** (keine echten Netz-/Twitch-Aufrufe, kein Bezug auf konkrete Live-Daten).
- Neue testbare Logik als **UMD-Modul** in `renderer/lib/` nach Vorbild `renderer/lib/backoff.js` (läuft im Browser UND unter Node).
- **Kein Remote-Code-Nachladen zur Laufzeit:** vaft liegt als gepinnte Datei `vendor/vaft.js` im Repo.
- Adblock-Einstellung heißt überall `adblockEnabled`, Default `true`.
- Der Vendor-File `vendor/vaft.js` bleibt **unverändert** (austauschbar); unser eigener Code (Wrapper/Hook) lebt nur im Injektionscode.
- Fehler in der Injektion dürfen **nie** den Player kaputtmachen: alles in try/catch, im Fehlerfall läuft die App normal (mit Werbung) weiter.
- IPC-Kanalname für Werbe-Status: `adblock-state`. Message-Format aus der Seite: `window.postMessage({ source: 'twitchdual-adblock', phase: 'start' | 'end' }, '*')`.
- Commit-Messages enden mit `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Zielversion Release: **v1.2.0** über den Ablauf in `docs/TODO.md`.

---

### Task 1: Adblock-Zustandsmaschine (`ad-overlay-state.js`)

DOM-freie Logik für den Fallback: entscheidet aus den Ereignissen `adStart`/`adEnd`/`tick`, ob das Overlay sichtbar ist und ob gemutet werden soll — inkl. Merken/Wiederherstellen des vorherigen Mute-Zustands und 120-s-Watchdog.

**Files:**
- Create: `renderer/lib/ad-overlay-state.js`
- Test: `test/ad-overlay-state.test.js`

**Interfaces:**
- Consumes: nichts (reines Modul).
- Produces: `AdOverlayState` (UMD-Export). Konstruktor: `createAdOverlayState({ watchdogMs = 120000 } = {})`. Methoden:
  - `adStart(currentlyMuted: boolean) -> void` — Werbung beginnt; merkt `currentlyMuted` als Wiederherstellungswert, nur beim Übergang idle→ad.
  - `adEnd() -> void` — Werbung endet; zurück zu idle.
  - `tick(nowMs: number) -> void` — Zeitgeber; löst Watchdog aus, wenn seit `adStart` mehr als `watchdogMs` vergangen ist.
  - `get overlayVisible() -> boolean` — true, solange im Ad-Zustand.
  - `get shouldMute() -> boolean` — true, solange im Ad-Zustand.
  - `get restoreMuted() -> boolean` — Mute-Zustand, der bei adEnd/Watchdog wiederhergestellt werden soll (der zum adStart gemerkte Wert).
  - `get active() -> boolean` — Alias für `overlayVisible` (im Ad-Zustand?).

- [ ] **Step 1: Failing Test schreiben**

Datei `test/ad-overlay-state.test.js`:

```javascript
const { test } = require('node:test');
const assert = require('node:assert');
const createAdOverlayState = require('../renderer/lib/ad-overlay-state');

test('startet inaktiv', () => {
  const s = createAdOverlayState();
  assert.equal(s.overlayVisible, false);
  assert.equal(s.shouldMute, false);
  assert.equal(s.active, false);
});

test('adStart aktiviert Overlay + Mute', () => {
  const s = createAdOverlayState();
  s.adStart(false);
  assert.equal(s.overlayVisible, true);
  assert.equal(s.shouldMute, true);
});

test('adEnd deaktiviert wieder', () => {
  const s = createAdOverlayState();
  s.adStart(false);
  s.adEnd();
  assert.equal(s.overlayVisible, false);
  assert.equal(s.shouldMute, false);
});

test('restoreMuted merkt sich Zustand beim adStart', () => {
  const s = createAdOverlayState();
  s.adStart(true);   // Nutzer hatte selbst gemutet
  assert.equal(s.restoreMuted, true);
  const s2 = createAdOverlayState();
  s2.adStart(false); // Nutzer hatte Ton an
  assert.equal(s2.restoreMuted, false);
});

test('doppeltes adStart überschreibt restoreMuted NICHT', () => {
  const s = createAdOverlayState();
  s.adStart(false);       // Ton war an -> merken: false
  s.adStart(true);        // kommt während der Werbung (schon gemutet) -> ignorieren
  assert.equal(s.restoreMuted, false);
});

test('tick löst Watchdog nach watchdogMs aus', () => {
  const s = createAdOverlayState({ watchdogMs: 1000 });
  s.adStart(false);
  s.tick(500);   // adStart-Zeit ist tick-relativ: erster tick setzt Referenz? -> nein, siehe Impl
  assert.equal(s.overlayVisible, true);
  s.tick(1600);
  assert.equal(s.overlayVisible, false);
});

test('adEnd nach Watchdog ist harmlos', () => {
  const s = createAdOverlayState({ watchdogMs: 1000 });
  s.adStart(false);
  s.tick(1000);   // erster tick primt die Referenz (raeumt noch nicht auf)
  s.tick(2500);   // 1500 ms spaeter >= watchdogMs -> Aufraeumen
  assert.equal(s.overlayVisible, false);
  s.adEnd(); // darf nicht werfen, bleibt inaktiv
  assert.equal(s.overlayVisible, false);
});
```

**Wichtig (Watchdog-Semantik):** Der erste `tick` nach `adStart` setzt nur die
Zeit-Referenz und räumt NICHT auf — erst ein späterer `tick`, dessen `nowMs`
mindestens `watchdogMs` über der Referenz liegt, beendet den Ad-Zustand. So
funktioniert es mit echten `Date.now()`-Timestamps (Task 5 ruft `tick(Date.now())`
per 1-s-Intervall). Kein Absolut-Zeit-Vergleich (`nowMs >= watchdogMs`) — der
würde unter echter Uhr sofort beim ersten Tick auslösen.

- [ ] **Step 2: Test laufen lassen, Fehlschlag prüfen**

Run: `npm test`
Expected: FAIL — `Cannot find module '../renderer/lib/ad-overlay-state'`.

- [ ] **Step 3: Modul implementieren**

Datei `renderer/lib/ad-overlay-state.js`:

```javascript
// DOM-freie Zustandsmaschine fuer den Werbe-Overlay-Fallback.
// UMD wie backoff.js: laeuft im Browser (<script>) und unter Node -> testbar.
//
// Zustaende: idle <-> ad. Im ad-Zustand ist Overlay sichtbar und der Player
// soll gemutet sein. Beim Uebergang idle->ad wird der aktuelle Mute-Zustand
// gemerkt (restoreMuted) und beim Uebergang ad->idle wiederhergestellt.
// Watchdog: bleibt adEnd aus, raeumt tick() nach watchdogMs automatisch auf.

(function (root, factory) {
  if (typeof module === 'object' && module.exports) {
    module.exports = factory();
  } else {
    root.createAdOverlayState = factory();
  }
})(typeof self !== 'undefined' ? self : this, function () {
  function createAdOverlayState({ watchdogMs = 120000 } = {}) {
    let inAd = false;
    let restoreMuted = false;
    let startTick = null; // Zeit (ms) des ersten tick nach adStart

    return {
      adStart(currentlyMuted) {
        if (inAd) return;        // schon in Werbung -> restoreMuted nicht ueberschreiben
        inAd = true;
        restoreMuted = !!currentlyMuted;
        startTick = null;
      },
      adEnd() {
        inAd = false;
        startTick = null;
      },
      tick(nowMs) {
        if (!inAd) return;
        if (startTick === null) { startTick = nowMs; return; }
        if (nowMs - startTick >= watchdogMs) {
          inAd = false;
          startTick = null;
        }
      },
      get overlayVisible() { return inAd; },
      get shouldMute() { return inAd; },
      get restoreMuted() { return restoreMuted; },
      get active() { return inAd; }
    };
  }
  return createAdOverlayState;
});
```

Hinweis zum Watchdog-Test: `tick(500)` ist der erste tick nach `adStart` und setzt `startTick=500`. `tick(1600)` liegt 1100 ms später ≥ `watchdogMs` (1000) → Aufräumen. Passt zu den Assertions.

- [ ] **Step 4: Test laufen lassen, Erfolg prüfen**

Run: `npm test`
Expected: PASS — Gesamtzahl **66 Tests** (59 + 7 neue), 0 fail.

- [ ] **Step 5: Commit**

```bash
git add renderer/lib/ad-overlay-state.js test/ad-overlay-state.test.js
git commit -m "feat: DOM-freie Adblock-Overlay-Zustandsmaschine + Tests"
```

---

### Task 2: Adblock-Einstellung in Main + IPC-Gerüst

Persistente Einstellung `adblockEnabled` (Default `true`) in electron-store, per IPC les-/setzbar, und das IPC-Relay `adblock-state` von einem beliebigen (Sub-)Frame ans Video-Fenster. Noch ohne Injektion — nur Verdrahtung.

**Files:**
- Modify: `main.js` (store-defaults, neue IPC-Handler, Video-Fenster `webPreferences`)
- Modify: `preload.js` (Bridge-Methoden für Settings + Adblock-State-Empfang)

**Interfaces:**
- Consumes: `store` aus `main.js`, bestehendes `broadcast`/`videoWin`.
- Produces:
  - IPC `get-adblock-enabled` (invoke) → `boolean`.
  - IPC `set-adblock-enabled` (invoke, arg `boolean`) → `{ ok: true, enabled: boolean }`.
  - IPC `adblock-state` (send, arg `{ phase: 'start'|'end' }`, darf aus Subframe kommen) → relayed als `webContents.send('adblock-state', { phase })` an `videoWin`.
  - Preload-Bridge: `window.twitchDual.getAdblockEnabled()`, `window.twitchDual.setAdblockEnabled(bool)`, `window.twitchDual.onAdblockState(cb)`.

- [ ] **Step 1: store-Default ergänzen**

In `main.js` im `new Store({ defaults: { … } })` (nach `chatPrefs`-Zeile) ergänzen:

```javascript
    chatPrefs: { showTimestamps: true, showBadges: true },
    adblockEnabled: true
```

- [ ] **Step 2: `nodeIntegrationInSubFrames` fürs Video-Fenster setzen**

In `main.js` in `createWindows()` beim `videoWin`-`webPreferences`-Block ergänzen (nur Video-Fenster, nicht Chat):

```javascript
  videoWin = new BrowserWindow({
    ...vb,
    title: 'TwitchDual — Video',
    backgroundColor: '#0e0e10',
    webPreferences: {
      preload: path.join(__dirname, 'preload.js'),
      contextIsolation: true,
      nodeIntegration: false,
      nodeIntegrationInSubFrames: true
    }
  });
```

- [ ] **Step 3: IPC-Handler ergänzen**

In `main.js` bei den übrigen `ipcMain`-Handlern (z. B. nach `save-chat-prefs`) einfügen:

```javascript
// Adblock-Einstellung (persistent, Default an).
ipcMain.handle('get-adblock-enabled', () => store.get('adblockEnabled', true));
ipcMain.handle('set-adblock-enabled', (_evt, enabled) => {
  const val = !!enabled;
  store.set('adblockEnabled', val);
  return { ok: true, enabled: val };
});

// Werbe-Status aus dem Player-iframe -> ans Video-Fenster relayen.
ipcMain.on('adblock-state', (_evt, payload) => {
  const phase = payload && payload.phase;
  if ((phase === 'start' || phase === 'end') && videoWin && !videoWin.isDestroyed()) {
    videoWin.webContents.send('adblock-state', { phase });
  }
});
```

- [ ] **Step 4: Preload-Bridge erweitern**

In `preload.js` im `exposeInMainWorld('twitchDual', { … })`-Objekt (vor der schließenden `})`) ergänzen — Komma nach `channelVods`-Zeile nicht vergessen:

```javascript
  channelVods: (login, limit) => ipcRenderer.invoke('channel-vods', { login, limit }),

  // Adblock: Einstellung lesen/setzen + Werbe-Status empfangen (Video-Fenster).
  getAdblockEnabled: () => ipcRenderer.invoke('get-adblock-enabled'),
  setAdblockEnabled: (enabled) => ipcRenderer.invoke('set-adblock-enabled', enabled),
  onAdblockState: (cb) => {
    ipcRenderer.on('adblock-state', (_e, payload) => cb(payload));
  }
```

- [ ] **Step 5: Tests laufen lassen (Regression)**

Run: `npm test`
Expected: PASS — **66 Tests**, 0 fail (keine neuen Tests hier; Main/Preload sind Electron-Laufzeit und werden per Smoke-Test in Task 6 geprüft).

- [ ] **Step 6: Commit**

```bash
git add main.js preload.js
git commit -m "feat: adblockEnabled-Setting + adblock-state IPC-Relay (Subframes aktiv)"
```

---

### Task 3: vaft gepinnt einbetten (`vendor/vaft.js`) + Build-Einschluss

Das aktiv gepflegte vaft-Skript als feste Datei ins Repo legen und in den Build aufnehmen. Kein Laufzeit-Download.

**Files:**
- Create: `vendor/vaft.js` (heruntergeladene Vendor-Datei, unverändert)
- Create: `vendor/README.md` (Herkunft, Version, Update-Prozedur)
- Modify: `package.json` (`build.files` um `"vendor/**/*"` erweitern)

**Interfaces:**
- Consumes: nichts.
- Produces: Datei `vendor/vaft.js`, die Task 4 als String liest und injiziert.

- [ ] **Step 1: vaft-Skript herunterladen**

Aktuelle vaft-Rohdatei aus dem gepflegten Fork holen (Userscript-Variante). Quelle: `https://github.com/ryanbr/TwitchAdSolutions` → Datei `vaft/vaft.user.js` (Raw-URL). Speichern als `vendor/vaft.js`. Datei **nicht editieren**.

Beim Ausführen: exakte Raw-URL und Version zum Zeitpunkt der Umsetzung ermitteln (Release-Tag notieren) — die Datei muss die Userscript-Logik enthalten, die den Player-Worker hookt und M3U8/Player-Typ tauscht. Falls die Rohdatei einen Userscript-Header (`// ==UserScript==`) enthält, bleibt er drin (schadet der Injektion nicht).

- [ ] **Step 2: `vendor/README.md` schreiben**

```markdown
# vendor/vaft.js

Fremdcode: Twitch-Adblock-Skript **vaft** aus dem gepflegten Fork
https://github.com/ryanbr/TwitchAdSolutions (Ordner `vaft/`).

- **Bezogen am:** <DATUM beim Umsetzen eintragen>
- **Version/Tag:** <Release-Tag eintragen>
- **Lizenz:** siehe Kopf der Datei / Upstream-Repo.

## Warum gepinnt?
Kein Laufzeit-Download (Remote-Code-Risiko). Die Datei wird unverändert
per Preload in den `player.twitch.tv`-iframe injiziert (siehe `preload.js`).

## Aktualisieren
1. Neue `vaft.user.js` aus dem Fork holen, hier als `vaft.js` ersetzen.
2. Datum/Version oben aktualisieren.
3. Smoke-Test: Live-Channel mit Werbung laden, prüfen dass Werbung
   übersprungen/überbrückt wird und der Chat-Sync weiter läuft.
4. Als normales App-Release (Version bump) ausliefern.
```

- [ ] **Step 3: Build-Einschluss ergänzen**

In `package.json` das `build.files`-Array um vendor erweitern:

```json
    "files": [
      "main.js",
      "preload.js",
      "renderer/**/*",
      "src/**/*",
      "vendor/**/*"
    ],
```

- [ ] **Step 4: Tests laufen lassen (Regression)**

Run: `npm test`
Expected: PASS — **66 Tests**, 0 fail.

- [ ] **Step 5: Commit**

```bash
git add vendor/vaft.js vendor/README.md package.json
git commit -m "chore: vaft gepinnt in vendor/ + in Build aufgenommen"
```

---

### Task 4: vaft-Injektion + Werbe-Hook im Preload

Der Preload läuft ab Task 2 auch in Subframes. Erkennt er den `player.twitch.tv`-iframe, injiziert er (nur wenn `adblockEnabled`) `vendor/vaft.js` in die Main World und meldet Werbe-Start/-Ende an Main.

**Files:**
- Modify: `preload.js` (Subframe-Zweig: vaft injizieren + Werbe-Hook)

**Interfaces:**
- Consumes: `window.twitchDual.getAdblockEnabled()` (Task 2), IPC `adblock-state` (Task 2), Datei `vendor/vaft.js` (Task 3).
- Produces: injiziert vaft; postet `window.postMessage({ source: 'twitchdual-adblock', phase })` aus der Seite und leitet es per `ipcRenderer.send('adblock-state', { phase })` weiter.

- [ ] **Step 1: Subframe-Erkennung + Injektion im Preload ergänzen**

In `preload.js` — der bestehende `contextBridge`-Block gilt für den Hauptframe. Am **Ende der Datei** den Subframe-Zweig ergänzen. `require` von `fs`/`path`/`webFrame` oben mitnehmen:

```javascript
const { contextBridge, ipcRenderer, webFrame } = require('electron');
const fs = require('fs');
const path = require('path');
```

Und am Dateiende:

```javascript
// --- Werbe-Blocker: nur im Twitch-Player-iframe ------------------------------
// Der Preload laeuft (Video-Fenster, nodeIntegrationInSubFrames) auch in
// Subframes. Im player.twitch.tv-iframe injizieren wir vaft in die Main World
// und leiten Werbe-Start/-Ende an Main weiter. Fehler duerfen den Player nie
// kaputtmachen -> alles in try/catch, im Zweifel passiert nichts.
(async function setupAdblock() {
  try {
    const host = location.hostname || '';
    if (!/(^|\.)twitch\.tv$/.test(host)) return;      // nur Twitch-Frames
    if (host === 'localhost') return;                 // unsere eigene Seite nicht

    const enabled = await ipcRenderer.invoke('get-adblock-enabled');
    if (!enabled) return;

    // Werbe-Signale der Seite (aus vaft-Wrapper) an Main relayen.
    window.addEventListener('message', (e) => {
      const d = e && e.data;
      if (d && d.source === 'twitchdual-adblock' &&
          (d.phase === 'start' || d.phase === 'end')) {
        ipcRenderer.send('adblock-state', { phase: d.phase });
      }
    });

    const vaftPath = path.join(__dirname, 'vendor', 'vaft.js');
    const vaftSrc = fs.readFileSync(vaftPath, 'utf8');

    // Wrapper: exponiert postMessage-Signal fuer unseren Hook, laedt dann vaft.
    // vaft loggt Ad-Erkennung; wir beobachten diese Signale defensiv ueber eine
    // von uns definierte Bruecke window.__twitchDualAd(phase).
    const bootstrap = `
      (function(){
        window.__twitchDualAd = function(phase){
          try { window.postMessage({ source: 'twitchdual-adblock', phase: phase }, '*'); } catch(e){}
        };
      })();
    ` + vaftSrc;

    await webFrame.executeJavaScript(bootstrap);
  } catch (e) {
    // Bewusst schlucken: lieber Werbung als kaputter Player.
    try { console.error('[TwitchDual] Adblock-Injektion fehlgeschlagen:', e && e.message); } catch (_) {}
  }
})();
```

- [ ] **Step 2: Werbe-Signal an vaft anbinden**

vaft meldet Ad-Phasen intern (Konsolen-Logs/State beim Playlist-Tausch). Um daraus `start`/`end` zu erzeugen, wird im `bootstrap`-String vor dem vaft-Quelltext ein leichter Hook auf die von vaft genutzte Meldung gelegt. Konkret die von vaft beim Umschalten ausgegebene Log-Zeile abfangen, indem `console.log`/`console.info` im injizierten Scope umschlossen werden:

Im `bootstrap`-String die Definition von `window.__twitchDualAd` ergänzen um einen console-Wrapper (ersetzt den `(function(){ … })();`-Block):

```javascript
    const bootstrap = `
      (function(){
        window.__twitchDualAd = function(phase){
          try { window.postMessage({ source: 'twitchdual-adblock', phase: phase }, '*'); } catch(e){}
        };
        var _log = console.log.bind(console);
        console.log = function(){
          try {
            var msg = Array.prototype.join.call(arguments, ' ');
            if (/ad segment|midroll|commercial|purhcasing|stream is ad|adblock/i.test(msg)) {
              window.__twitchDualAd('start');
            }
            if (/clean stream|main stream|ad(s)? (over|ended|finished)|switching back/i.test(msg)) {
              window.__twitchDualAd('end');
            }
          } catch(e){}
          return _log.apply(console, arguments);
        };
      })();
    ` + vaftSrc;
```

Hinweis: Die Regex-Marker sind heuristisch an vafts Log-Ausgaben angelehnt; beim Smoke-Test (Task 6) anhand der realen vaft-Konsole verifizieren und, falls nötig, die Marker anpassen. Das Overlay ist ohnehin nur Fallback — bleibt der `start` einmal aus, greift weiterhin vafts eigener Ad-Skip; bleibt `end` aus, räumt der 120-s-Watchdog (Task 1/5) auf.

- [ ] **Step 3: Tests laufen lassen (Regression)**

Run: `npm test`
Expected: PASS — **66 Tests**, 0 fail (Preload ist Laufzeit; Verifikation per Smoke-Test in Task 6).

- [ ] **Step 4: Commit**

```bash
git add preload.js
git commit -m "feat: vaft in Player-iframe injizieren + Werbe-Signal an Main relayen"
```

---

### Task 5: Overlay + Mute im Video-Fenster verdrahten

Zustandsmaschine aus Task 1 im Video-Fenster einbinden: `adblock-state`-Events → Overlay ein/aus + Player mute/unmute mit Wiederherstellung, dazu ein `tick`-Timer für den Watchdog.

**Files:**
- Modify: `renderer/video/index.html` (Overlay-Element + `<script>` für `ad-overlay-state.js`)
- Modify: `renderer/video/video.js` (State-Maschine anbinden)

**Interfaces:**
- Consumes: `window.createAdOverlayState` (Task 1, via `<script>`), `window.twitchDual.onAdblockState` (Task 2), `player.setMuted()/getMuted()` (Twitch-Embed-API).
- Produces: sichtbares Overlay + Mute-Verhalten (keine weiteren Konsumenten).

- [ ] **Step 1: Overlay-Element + Script einbinden (`index.html`)**

In `renderer/video/index.html` im `#player`-Div nach dem `#hint`-Div ergänzen:

```html
  <div id="player">
    <div id="hint">Channel-Name für LIVE oder VOD-Link/ID eingeben und „Laden" drücken — oder ☰ Home für Favoriten.</div>
    <div id="ad-overlay" class="hidden">
      <div id="ad-overlay-box">
        <div id="ad-overlay-title">Werbung wird überbrückt …</div>
        <div id="ad-overlay-sub">Ton ist kurz stumm — geht gleich weiter.</div>
      </div>
    </div>
  </div>
```

Und im `<style>`-Block (vor `</style>`) ergänzen:

```css
    #ad-overlay {
      position: absolute; inset: 0; z-index: 5;
      display: flex; align-items: center; justify-content: center;
      background: #0e0e10; color: #efeff1; text-align: center; padding: 24px;
    }
    #ad-overlay.hidden { display: none; }
    #ad-overlay-title { font-size: 16px; font-weight: 600; margin-bottom: 6px; }
    #ad-overlay-sub { font-size: 13px; color: #adadb8; }
```

Und das State-Modul vor `video.js` laden:

```html
  <script src="https://embed.twitch.tv/embed/v1.js"></script>
  <script src="../lib/ad-overlay-state.js"></script>
  <script src="video.js"></script>
  <script src="home.js"></script>
```

Hinweis Pfad: `index.html` liegt in `renderer/video/`, das Modul in `renderer/lib/` → `../lib/ad-overlay-state.js`. Der lokale Server liefert `renderer/` als Root aus (siehe `main.js` `startServer(path.join(__dirname, 'renderer'))`), relative Pfade funktionieren.

- [ ] **Step 2: State-Maschine in `video.js` anbinden**

In `renderer/video/video.js` nach den bestehenden `const $…`-Deklarationen (oben) ergänzen:

```javascript
const $adOverlay = document.getElementById('ad-overlay');
const adState = window.createAdOverlayState ? window.createAdOverlayState() : null;

function renderAdOverlay() {
  if (!adState || !$adOverlay) return;
  $adOverlay.classList.toggle('hidden', !adState.overlayVisible);
  if (!player) return;
  try {
    if (adState.shouldMute) {
      player.setMuted(true);
    } else {
      // Werbe-Ende: gemerkten Mute-Zustand wiederherstellen.
      player.setMuted(adState.restoreMuted);
    }
  } catch (e) {}
}

// Werbe-Status aus dem Player-iframe (via Main-Relay).
if (window.twitchDual.onAdblockState) {
  window.twitchDual.onAdblockState((payload) => {
    if (!adState) return;
    const phase = payload && payload.phase;
    if (phase === 'start') {
      let muted = false;
      try { muted = !!(player && player.getMuted && player.getMuted()); } catch (e) {}
      adState.adStart(muted);
    } else if (phase === 'end') {
      adState.adEnd();
    }
    renderAdOverlay();
  });
}

// Watchdog-Timer: raeumt Overlay/Mute auf, falls kein 'end' kommt.
setInterval(() => {
  if (!adState) return;
  const wasActive = adState.overlayVisible;
  adState.tick(Date.now());
  if (wasActive && !adState.overlayVisible) renderAdOverlay();
}, 1000);
```

- [ ] **Step 3: Tests laufen lassen (Regression)**

Run: `npm test`
Expected: PASS — **66 Tests**, 0 fail.

- [ ] **Step 4: Commit**

```bash
git add renderer/video/index.html renderer/video/video.js
git commit -m "feat: Werbe-Overlay + Mute im Video-Fenster (Zustandsmaschine angebunden)"
```

---

### Task 6: Adblock-Schalter in der UI + manueller Smoke-Test

Ein sichtbarer An/Aus-Schalter (persistent) in der Video-Leiste, plus manuelle Verifikation des Gesamtverhaltens.

**Files:**
- Modify: `renderer/video/index.html` (Schalter im `#bar`)
- Modify: `renderer/video/video.js` (Schalter-Logik)

**Interfaces:**
- Consumes: `window.twitchDual.getAdblockEnabled()` / `setAdblockEnabled()` (Task 2).
- Produces: UI-Toggle (keine weiteren Konsumenten). Änderung greift nach Neuladen des Players (Injektion passiert beim iframe-Aufbau).

- [ ] **Step 1: Schalter ins Markup (`index.html`)**

In `renderer/video/index.html` im `#bar`-Div nach dem `#load`-Button ergänzen:

```html
    <button id="load">Laden</button>
    <button id="adblock-toggle" title="Werbe-Blocker an/aus">🛡 Ads</button>
    <span id="status">bereit</span>
```

Und im `<style>` ergänzen:

```css
    #adblock-toggle {
      -webkit-app-region: no-drag;
      padding: 8px 10px; border: 1px solid #3a3a3d; border-radius: 6px;
      background: #0e0e10; color: #adadb8; font-size: 13px; cursor: pointer;
      white-space: nowrap;
    }
    #adblock-toggle.on { background: #1f3d1f; border-color: #3fa34d; color: #eafbea; }
```

- [ ] **Step 2: Schalter-Logik (`video.js`)**

In `renderer/video/video.js` am Dateiende ergänzen:

```javascript
// --- Adblock-Schalter -------------------------------------------------------
const $adblock = document.getElementById('adblock-toggle');

function renderAdblockBtn(enabled) {
  if (!$adblock) return;
  $adblock.classList.toggle('on', !!enabled);
  $adblock.textContent = enabled ? '🛡 Ads: an' : '🛡 Ads: aus';
}

if ($adblock) {
  window.twitchDual.getAdblockEnabled().then(renderAdblockBtn).catch(() => {});
  $adblock.addEventListener('click', async () => {
    try {
      const cur = await window.twitchDual.getAdblockEnabled();
      const res = await window.twitchDual.setAdblockEnabled(!cur);
      renderAdblockBtn(res.enabled);
      setStatus(res.enabled
        ? 'Werbe-Blocker an — beim nächsten Laden aktiv.'
        : 'Werbe-Blocker aus — beim nächsten Laden.');
    } catch (e) {}
  });
}
```

- [ ] **Step 3: Tests laufen lassen (Regression)**

Run: `npm test`
Expected: PASS — **66 Tests**, 0 fail.

- [ ] **Step 4: Manueller Smoke-Test**

Run: `npm start`

Prüfen:
1. **Blockwirkung:** Live-Channel laden, der regelmäßig Werbung schaltet. Erwartung: Werbung wird übersprungen; falls doch, erscheint kurz das Overlay „Werbung wird überbrückt …" und der Ton ist stumm, danach geht es weiter.
2. **Konsole (vaft):** DevTools des Video-Fensters öffnen, vafts Log-Zeilen ansehen. Stimmen die `start`/`end`-Regex-Marker aus Task 4 Step 2 **nicht** mit den realen Meldungen überein → Marker anpassen, `preload.js` neu committen (Amend an Task-4-Commit oder neuer Fix-Commit).
3. **Chat-Sync:** VOD laden, prüfen dass `getCurrentTime`-Replay-Sync weiter funktioniert (Chat läuft mit dem Player).
4. **Schalter aus:** 🛡 Ads ausschalten, Player neu laden → Originalverhalten (Werbung wie in v1.1.0), kein Overlay.
5. **Watchdog:** (optional) Falls Overlay mal hängt, verschwindet es nach spätestens 120 s automatisch.

- [ ] **Step 5: Commit**

```bash
git add renderer/video/index.html renderer/video/video.js
git commit -m "feat: Adblock-Schalter (persistent) in der Video-Leiste"
```

---

### Task 7: Doku aktualisieren + Release v1.2.0

Roadmap/Update-Prozedur dokumentieren und v1.2.0 über den bestehenden Ablauf veröffentlichen.

**Files:**
- Modify: `docs/TODO.md` (Adblock unter „Erledigt", vaft-Update-Hinweis)
- Modify: `README.md` (kurzer Adblock-Abschnitt)
- Modify: `package.json` (`version` → `1.2.0`)

**Interfaces:**
- Consumes: alle vorherigen Tasks.
- Produces: Release-Artefakte.

- [ ] **Step 1: `docs/TODO.md` ergänzen**

Unter „## Erledigt" einen Block ergänzen:

```markdown
**Werbe-Blocker (v1.2.0)**
- vaft (gepinnt in `vendor/vaft.js`, Fork ryanbr/TwitchAdSolutions) wird per
  Preload in den `player.twitch.tv`-iframe injiziert und überspringt Werbung
  (Playlist-/Player-Typ-Tausch). Nur wenn `adblockEnabled` (Default an).
- Fallback bei durchgekommener Werbung: Overlay „Werbung wird überbrückt …"
  + Mute, gesteuert von DOM-freier Zustandsmaschine
  (`renderer/lib/ad-overlay-state.js`, unit-getestet) mit 120-s-Watchdog.
- Adblock-Schalter (🛡 Ads) in der Video-Leiste, persistent.
- **vaft aktualisieren:** siehe `vendor/README.md` (Datei ersetzen, Smoke-Test,
  als App-Release ausliefern).
```

- [ ] **Step 2: `README.md` ergänzen**

Passenden kurzen Abschnitt einfügen (Platzierung nach dem Feature-Überblick):

```markdown
## Werbe-Blocker

TwitchDual injiziert das gepflegte **vaft**-Skript in den Twitch-Player und
überspringt so die serverseitig eingebettete Werbung. Kommt doch mal Werbung
durch, wird sie kurz per Overlay + Stummschaltung überbrückt. Über den
Schalter **🛡 Ads** in der Leiste lässt sich der Blocker abschalten (die
Einstellung wird gemerkt; sie greift beim nächsten Laden des Players).

Der Blocker ist für die private Nutzung gedacht. vaft ist Fremdcode
(`vendor/vaft.js`); Aktualisierung siehe `vendor/README.md`.
```

- [ ] **Step 3: Version bumpen**

In `package.json`: `"version": "1.1.0"` → `"version": "1.2.0"`.

- [ ] **Step 4: Tests laufen lassen (letzte Regression)**

Run: `npm test`
Expected: PASS — **66 Tests**, 0 fail.

- [ ] **Step 5: Commit + Merge-freundlicher Release-Commit**

```bash
git add docs/TODO.md README.md package.json
git commit -m "release: v1.2.0 – Werbe-Blocker (vaft + Overlay-Fallback)"
```

- [ ] **Step 6: Release bauen und veröffentlichen**

Nach dem Ablauf in `docs/TODO.md`:

```bash
npm run dist
```

Dann im Ordner `dist/installer` die EXE + Blockmap auf Bindestrich-Namen bringen und Release anlegen:

```bash
gh release create v1.2.0 \
  "TwitchDual-Setup-1.2.0.exe" \
  "TwitchDual-Setup-1.2.0.exe.blockmap" \
  latest.yml
```

(Alternativ `npm run release` mit gesetztem `GH_TOKEN`.) Danach holen sich installierte Apps (Bruder) das Update automatisch.

- [ ] **Step 7: Tag/Push prüfen**

```bash
git push
git push --tags
```

Sicherstellen, dass `master` und der `v1.2.0`-Tag/Release auf GitHub stehen und `latest.yml` die Version `1.2.0` referenziert.

---

## Selbst-Review gegen die Spec

- **vaft-Injektion (Spec §Lösung 1):** Tasks 3+4. ✓
- **Overlay + Mute Fallback (Spec §Lösung 2):** Tasks 1+5. ✓
- **`vendor/vaft.js` gepinnt, kein Remote-Load (Spec §Komponenten):** Task 3. ✓
- **`preload.js` Subframe-Injektion + Relay (Spec):** Tasks 2+4. ✓
- **`main.js` `nodeIntegrationInSubFrames`, IPC-Relay, Setting (Spec):** Task 2. ✓
- **`ad-overlay-state.js` DOM-frei, unit-getestet, Watchdog (Spec §Komponenten/Tests):** Task 1. ✓
- **Overlay + Schalter in video.js/index.html (Spec):** Tasks 5+6. ✓
- **Fehlerbehandlung: try/catch, Watchdog, Mute-Restore, Schalter-aus (Spec §Fehlerbehandlung):** Task 4 Step 1 (try/catch), Task 1 (Watchdog/Restore), Task 2+6 (Schalter). ✓
- **Tests: 59 bleiben grün, neue Unit-Tests für State-Maschine (Spec §Tests):** jede Task endet mit `npm test`; Task 1 fügt 7 Tests hinzu. ✓
- **Release v1.2.0 über bestehenden Ablauf (Spec §Release):** Task 7. ✓

Kein Spec-Punkt ohne Task. Namen konsistent (`adblockEnabled`, `adblock-state`, `createAdOverlayState`, `phase: 'start'|'end'`, `source: 'twitchdual-adblock'`) über Tasks hinweg.

**Bekannte Umsetzungs-Unschärfe (bewusst):** Die `start`/`end`-Log-Marker in Task 4 Step 2 sind heuristisch und werden im Smoke-Test (Task 6 Step 4.2) gegen die reale vaft-Konsole verifiziert und ggf. angepasst. Das ist kein Blocker: vafts eigener Ad-Skip funktioniert unabhängig davon; das Overlay ist nur der abgesicherte Fallback.
